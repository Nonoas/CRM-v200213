spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:data/mycrm.db

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: indi.nonoas.crm.beans

  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


logging:
  level:
    indi:
      nonoas:
        crm:
          dao: warn

